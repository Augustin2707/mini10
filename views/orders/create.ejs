<%- include('../partials/header', { title: 'Nouvelle Commande - Mini10' }) %>

<div class="container mx-auto px-4 py-6 max-w-2xl">
    <!-- En-tête -->
    <div class="mb-8">
        <div class="flex items-center justify-between">
            <h1 class="text-3xl font-bold text-gray-900">
                <i class="mr-3">🛒</i>Nouvelle Commande
            </h1>
            <a href="/orders" 
               class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 transition-colors duration-200">
                <i class="mr-2">←</i>Retour
            </a>
        </div>
        <p class="text-gray-600 mt-2">Remplissez les informations pour passer une nouvelle commande</p>
    </div>

    <!-- Formulaire -->
    <div class="bg-white rounded-lg shadow-md p-6">
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="rounded-md bg-red-50 p-4 mb-6 flash-message">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <i class="text-red-400">⚠️</i>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-sm font-medium text-red-800">Erreur</h3>
                        <div class="mt-2 text-sm text-red-700">
                            <p><%= error %></p>
                        </div>
                    </div>
                </div>
            </div>
        <% } %>

        <form action="/orders/create" method="POST" class="space-y-6">
            <!-- Produit -->
            <div>
                <label for="product_id" class="block text-sm font-medium text-gray-700 mb-2">
                    <i class="mr-2">📦</i>Produit
                </label>
                <select name="product_id" id="product_id" required
                        class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                    <% products.forEach(product => { %>
                        <option value="<%= product.product_id %>">
                            <%= product.name %> (Stock: <%= product.quantity %>)
                        </option>
                    <% }) %>
                </select>
            </div>

            <!-- Quantité -->
            <div>
                <label for="quantity" class="block text-sm font-medium text-gray-700 mb-2">
                    <i class="mr-2">🔢</i>Quantité
                </label>
                <input type="number" name="quantity" id="quantity" required min="1"
                       class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm"
                       placeholder="Quantité souhaitée">
            </div>

            <!-- Motif -->
            <div>
                <label for="motif" class="block text-sm font-medium text-gray-700 mb-2">
                    <i class="mr-2">📝</i>Motif de la commande
                </label>
                <textarea name="motif" id="motif" required rows="3"
                          class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm"
                          placeholder="Expliquez la raison de cette commande..."></textarea>
            </div>

            <!-- Commentaire livraison -->
            <div>
                <label for="delivery_comment" class="block text-sm font-medium text-gray-700 mb-2">
                    <i class="mr-2">💬</i>Commentaire de livraison (optionnel)
                </label>
                <textarea name="delivery_comment" id="delivery_comment" rows="2"
                          class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm"
                          placeholder="Instructions spéciales pour la livraison..."></textarea>
            </div>

            <!-- Boutons -->
            <div class="flex justify-end space-x-3 pt-4">
                <a href="/orders" 
                   class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 transition-colors duration-200">
                    Annuler
                </a>
                <button type="submit" 
                        class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200">
                    <i class="mr-2">🚀</i>Envoyer la Commande
                </button>
            </div>
        </form>
    </div>
</div>

<%- include('../partials/footer') %>