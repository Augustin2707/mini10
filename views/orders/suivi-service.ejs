<%- include('../partials/header') %>
<div class="container mt-5">
  <h2 class="text-center mb-4">📊 Mes Réceptions Confirmées
    <a href="/orders">Retour</a>
</h1>
  </h2>
  <table class="table table-striped table-hover">
    <thead class="table-dark">
      <tr>
        <th>ID Produit</th>
        <th>Produit</th>
        <th>Quantité</th>
        <th>Motif</th>
        <th>Date</th>
        <th>Auteur</th>
        <th>Statut</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% if (actions.length > 0) { %>
        <% actions.forEach(action => { %>
          <tr>
            <td><%= action.id_produit %></td>
            <td><%= action.produit %></td>
            <td><%= action.quantite %></td>
            <td><%= action.motif %></td>
            <td><%= action.date_heure %></td>
            <td><%= action.identification_auteur %></td>
            <td><span class="badge bg-success"><%= action.statut %></span></td>
            <td><span class="badge bg-info"><%= action.action %></span></td>
          </tr>
        <% }) %>
      <% } else { %>
        <tr><td colspan="8" class="text-center">Aucune réception confirmée.</td></tr>
      <% } %>
    </tbody>
  </table>
</div>
<%- include('../partials/footer') %>